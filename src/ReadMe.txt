1. Bootstrap and font-awesome have been used for good looking UI which must be configured in the machine where application is running.
2. for storing and retriving data we are using a npm package json-server which is a npm package and it uses json file to store and retrive data 
3. for implementing search contact feature we have used Ng2SearchPipeModule its an npm package